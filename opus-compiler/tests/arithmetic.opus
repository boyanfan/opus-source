// Test Case, where
    // This (starts with '//') is a comment in Opus and should be ignored by the compiler
    // Whitespaces (like the spaces before this line) should also be ignored

    12 - 34 * 56

{
    78 * 90 + ( 12345
    // This should be valid since newline characters in a closure (between [...], (...) and <...>) are whitespaces
    % 45678)
}

// Test case for the comment at the end of line