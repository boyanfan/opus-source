// Arithmetic operators
12 + 3.14 * -56.7 / 99! % 3 - 8

// Floating number, negative number, and negative floating numbers
[123.45, -789, -3.1415]

// Exclamation mark as a logical negation, as an factorial (after numeric), and as part of operator (`!=`)
!(12! != 144)

// All supported comparison operators and logical operators
[==, !=, >, <, >=, <=, &&, ||]

// Whitespace between operators and operand are not required, like, "(12!) != 3 && !true" can be written as
12!!=3 &&!true